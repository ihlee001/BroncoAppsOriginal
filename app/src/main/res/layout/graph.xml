<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".GraphActivity" >

    <LinearLayout
        android:id="@+id/graph1"
        android:layout_width="fill_parent"
        android:layout_height="350dp"
        android:orientation="vertical" />

    <Button
        android:id="@+id/velocity_motion_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/graph1"
        android:layout_alignParentBottom="true"
        android:onClick="drawVelocityActivity"
        android:text="Velocity" />

    <Button
        android:id="@+id/battery_consumption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/graph1"
        android:layout_alignTop="@+id/velocity_motion_activity"
        android:onClick="drawBatteryConsumption"
        android:text="Battery" />

</RelativeLayout>
